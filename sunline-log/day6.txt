day 6
10-15

TypeScript 的基本转换

.gitignore 文件
	在需要忽略某些文件不被添加到git缓存区时，配置该文件。但在配置后，使用 git add . 命令依旧会把需要忽略的文件添加进去，这是因为git的缓存的问题，此时可执行命令 
	git rm --cached -r 文件名/文件夹




iView 中render函数的使用

	- render 接受两个参数 (h, params)。 
		-- h 为渲染dom的一个方法，有三个参数：标签名，属性对象，内容数组。
		-- params 中包含了传入该组件的数据对象。